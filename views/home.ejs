<%- include('partials/header') %>

  <div class="jumbotron centered">
    <div class="container">
      <i class="fas fa-key fa-6x"></i>
      <h1 class="display-3 heading"><a href="/secrets">Secrets</a></h1>
      <p class="lead">Don't keep your secrets, share them anonymously!</p>
      <hr class="mb-10">
      <div class="my-5">
        <% if(!isAuthenticated){%>
          <p class="lead">For Submit Your Secret(s)</p>
          <a class="btn btn-light btn-lg mx-3" href="/register" role="button">Register</a>
          <a class="btn btn-dark btn-lg" href="/login" role="button">Login</a>
          <% } %>
      </div>
    </div>
  </div>

  <%- include('partials/footer') %>