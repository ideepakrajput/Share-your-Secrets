<%- include('partials/header') %>

<div class="container">
  <div class="row">
    <div class="col-md-6">
      <i class="fas fa-key fa-3x"></i
      ><a href="/" class="display-4 mx-1">Secrets</a>
    </div>
    <div class="col-md-6 text-right mt-3">
      <a class="btn btn-light btn-lg" href="/logout" role="button">Log Out</a>
      <a class="btn btn-dark btn-lg" href="/submit" role="button"
        >Submit a Secret</a
      >
    </div>
  </div>
</div>
<div class="jumbotron text-center">
  <div class="container">
    <i class="fas fa-key fa-6x"></i>
    <h1 class="display-5 heading">You've Discovered A Lots of Secret!</h1>
    <form action="/delete" method="post">
      <% usersWithSecrets.forEach(user=> { %> <% user.secrets.forEach(secret=> {
      %>
      <p class="secret-text"><%= secret %></p>
      <!-- <i name="selectedSecret" style="color: white;" class="bi bi-trash3" onClick=this.form.submit()></i> -->
      <% }) %> <% }) %>
    </form>
  </div>
</div>

<%- include('partials/footer') %>
